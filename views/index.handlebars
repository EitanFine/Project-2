<div class="container">
    <div class="row"></div>
    <!-- Collect the nav links, forms, and other content for toggling -->

    {{!--
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
            {{#each categories}}
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Dropdown
                    <span class="caret"></span>
                </a>
                {{this.categoryName}}
            </li>
            {{/each}}
        </ul> --}}

        {{!-- <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                        <b>Categories</b>
                        <span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu">
                        {{#each categories}}
                        <li>
                            <a href="#">
                                {{this.categoryName}}

                            </a>
                        </li>
                        {{/each}}
                    </ul>

                </li> --}}
                {{!--
                <li>
                    <select class="form-control" name="category" id="category">
                        {{#each categories}}
                        <option value={{this.id}}>{{this.categoryName}}</option>
                        {{/each}}
                    </select>
                </li> --}}
{{!-- 
                <li>
                    <a href="#">
                        <b>How It Works</b>
                    </a>
                </li>
                <li>
                    <a href="#">
                        <b>About</b>
                    </a>
                </li>
                <li>
                    <a href="/newlisting">
                        <button type="button" class="btn btn-default navbar-btn">Post Your Item</button>
                    </a>
                </li>
            </ul>
        </div> --}}
        {{!-- <form class="navbar-form navbar-left">
            <div class="form-group">
                <input type="text" class="form-control" placeholder="Enter Zip Code">
            </div>
            <button type="submit" class="btn btn-default">Submit</button>
        </form>
        <ul class="nav navbar-nav navbar-right">
            <button type="button" class="btn btn-default navbar-btn">List Your Item</button>
        </ul>
    </div> --}}
    <!-- /.navbar-collapse -->
    <hr>

    <div class="panel panel-primary  ">
        <div class="panel-heading">
            {{!--
            <h3 class="panel-title">{{this.categoryName}}</h3> --}}
        </div>
        <style>
            .row.make-columns {
                -moz-column-width: 19em;
                -webkit-column-width: 19em;
                -moz-column-gap: 1em;
                -webkit-column-gap: 1em;
            }

            .row.make-columns>div {
                display: inline-block;
                padding: .5rem;
                width: 100%;
            }

            /* demo only* */

            .panel {
                display: inline-block;
                height: 100%;
                width: 100%;
            }
        </style>

        <br>
        {{!-- <div class="container"> --}}
            <div class="row make- columns">

                {{#each Item}}
                <div class="col-xs-6 col-md-4">
                    <div class="panel panel-default thumbnail">
                        <div class="panel-body">
                            {{!-- 1 - This is my panel with some content!
                            <br> --}}
                            <img style="height: 225px; width: 90%;" src="data:image/jpeg;base64, {{this.itemImage}}" alt="...">

                        </div>
                        <h3>{{this.itemName}}</h3>
                        <p>{{this.itemDescription}}</p>
                        <p>Per Day: {{this.itemPrice}}</p>
                        <a href="/iteminfo1/{{this.id}}" class="btn btn-primary" role="button">View Listing</a>

                    </div>
                </div>
                {{/each}}
            </div>
        {{!-- </div> --}}